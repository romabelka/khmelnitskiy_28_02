import React, { Component, PropTypes } from 'react'

class Counter extends Component {
    static propTypes = {

    };

    render() {
        return (
            <div>
                Hello world
            </div>
        )
    }
}

export default Counter